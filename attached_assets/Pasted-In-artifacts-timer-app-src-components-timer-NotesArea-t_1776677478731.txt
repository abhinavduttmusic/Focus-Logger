In `artifacts/timer-app/src/components/timer/NotesArea.tsx`, make these changes:

1. Add Sparkle icon to imports:
import { FileText, ChevronUp, Mic, Pause, Square, X, Pencil, Trash2, Repeat2, Sparkles } from "lucide-react";

2. Add these props to the NotesAreaProps interface:
onGenerateSummary: () => void;
isGeneratingSummary: boolean;

3. Add these to the destructured props:
onGenerateSummary,
isGeneratingSummary,

4. Find the header div containing the chevron button. It looks like:
<div className="flex items-center mb-4">
  <div className="flex items-center gap-2 text-muted-foreground font-medium flex-1">
    <FileText className="w-4 h-4" />
    <span>Session Notes & Goals</span>
  </div>
  <motion.button onClick={onToggleExpand} ...>
    <ChevronUp className="w-4 h-4" />
  </motion.button>
</div>

Add a sparkle button between the title div and the chevron button:
<motion.button
  onClick={onGenerateSummary}
  disabled={isGeneratingSummary}
  whileTap={{ scale: 0.88 }}
  className="w-8 h-8 rounded-full flex items-center justify-center bg-foreground/[0.05] hover:bg-foreground/[0.10] text-foreground/50 hover:text-foreground/80 transition-colors shrink-0 mr-1"
  aria-label="Generate AI summary"
  title="Generate your daily summary"
>
  {isGeneratingSummary ? (
    <motion.div
      animate={{ rotate: 360 }}
      transition={{ duration: 1, repeat: Infinity, ease: "linear" }}
    >
      <Sparkles className="w-4 h-4" />
    </motion.div>
  ) : (
    <Sparkles className="w-4 h-4" />
  )}
</motion.button>

Do not change anything else in NotesArea.tsx.

---

In `artifacts/timer-app/src/pages/home.tsx`, make these changes:

1. Add these state variables near the other state declarations:
const [summaryModalOpen, setSummaryModalOpen] = useState(false);
const [summaryText, setSummaryText] = useState("");
const [summaryLoading, setSummaryLoading] = useState(false);
const [summaryError, setSummaryError] = useState<string | null>(null);

2. Add this function after the state declarations:
const handleGenerateSummary = useCallback(async () => {
  setSummaryModalOpen(true);
  setSummaryLoading(true);
  setSummaryError(null);
  setSummaryText("");

  try {
    const { data: allSessions = [] } = useListSessions is not available here — instead read from the queryClient or pass sessions as a prop. 

    // Get today's sessions from the API
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // Build the prompt data from recorder clips and notes
    const sessionData = {
      date: new Date().toLocaleDateString("en-US", { weekday: "long", year: "numeric", month: "long", day: "numeric" }),
      notes: notes,
      clips: recorder.clips.map((c, i) => ({ label: c.label || `Recording ${i + 1}`, duration: c.durationSeconds })),
    };

    const prompt = `You are a Chief of Staff AI coach for a high-performing executive. Based on the session data below, write a daily debrief summary.

Voice: Professional, insightful, energized. Like a senior CoS briefing a C-suite executive.
Tone: Encouraging but grounded in reality. Call out what worked, what didn't, and give one sharp forward-looking directive.
Style: Short punchy sentences. Growth-mindset vocabulary. Collaborative perspective using "we" occasionally.

Guidelines:
- Lead with the most significant insight, not chronology
- Acknowledge genuine effort without empty praise
- Call out patterns honestly (late starts, long breaks, task switching, skewed focus/break ratio)
- Recognize momentum when sessions stack on the same task
- End with ONE specific, actionable directive for tomorrow — make it feel like a challenge, not a compliment
- Never use corporate jargon or clichés like "great job" or "keep it up"
- If breaks ran long, say so directly
- If the day started late, name it

Session data for ${sessionData.date}:
Notes: ${sessionData.notes || "No notes recorded"}
Voice recordings: ${sessionData.clips.length > 0 ? sessionData.clips.map(c => `"${c.label}"`).join(", ") : "None"}

Write the summary now. 3-4 paragraphs maximum. End with a one-line challenge for tomorrow.`;

    const response = await fetch("https://api.anthropic.com/v1/messages", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        model: "claude-sonnet-4-20250514",
        max_tokens: 1000,
        messages: [{ role: "user", content: prompt }],
      }),
    });

    const data = await response.json();
    const text = data.content?.[0]?.text || "Unable to generate summary.";
    setSummaryText(text);
  } catch (err) {
    setSummaryError("Something went wrong. Please try again.");
  } finally {
    setSummaryLoading(false);
  }
}, [notes, recorder.clips]);

3. Pass the new props to NotesArea:
onGenerateSummary={handleGenerateSummary}
isGeneratingSummary={summaryLoading}

4. Add this summary modal just before the closing </> of the home component return, after all other modals:

{summaryModalOpen && (
  <motion.div
    initial={{ opacity: 0 }}
    animate={{ opacity: 1 }}
    exit={{ opacity: 0 }}
    className="fixed inset-0 z-50 flex items-end justify-center"
    style={{ backgroundColor: 'rgba(0,0,0,0.5)' }}
    onClick={() => setSummaryModalOpen(false)}
  >
    <motion.div
      initial={{ y: 100, opacity: 0 }}
      animate={{ y: 0, opacity: 1 }}
      exit={{ y: 100, opacity: 0 }}
      transition={{ type: "spring", stiffness: 300, damping: 30 }}
      className="w-full max-w-lg bg-card rounded-t-3xl p-6 pb-10 max-h-[85vh] overflow-y-auto"
      onClick={(e) => e.stopPropagation()}
    >
      {/* Header */}
      <div className="flex items-center justify-between mb-6">
        <div className="flex items-center gap-2">
          <Sparkles className="w-5 h-5 text-primary" />
          <h2 className="text-lg font-semibold">Daily Debrief</h2>
        </div>
        <button
          onClick={() => setSummaryModalOpen(false)}
          className="w-8 h-8 rounded-full bg-secondary/60 flex items-center justify-center text-muted-foreground"
        >
          <X className="w-4 h-4" />
        </button>
      </div>

      {/* Date */}
      <p className="text-xs text-muted-foreground/50 font-medium uppercase tracking-widest mb-4">
        {new Date().toLocaleDateString("en-US", { weekday: "long", month: "long", day: "numeric" })}
      </p>

      {/* Content */}
      {summaryLoading && (
        <div className="flex flex-col items-center justify-center py-12 gap-4">
          <motion.div
            animate={{ rotate: 360 }}
            transition={{ duration: 1.5, repeat: Infinity, ease: "linear" }}
          >
            <Sparkles className="w-8 h-8 text-primary/60" />
          </motion.div>
          <p className="text-sm text-muted-foreground/60">Analysing your day...</p>
        </div>
      )}

      {summaryError && (
        <p className="text-sm text-destructive text-center py-8">{summaryError}</p>
      )}

      {summaryText && !summaryLoading && (
        <div className="prose prose-sm max-w-none">
          {summaryText.split('\n\n').map((para, i) => (
            <p key={i} className="text-[14px] leading-relaxed text-foreground/80 mb-4 last:mb-0">
              {para}
            </p>
          ))}
        </div>
      )}
    </motion.div>
  </motion.div>
)}

5. Add Sparkles and X to the lucide-react import in home.tsx if not already present.

Also add useListSessions to the imports in home.tsx:
import { useListSessions } from "@workspace/api-client-react";

Then update handleGenerateSummary to use the sessions data. Replace the comment about useListSessions with:
const sessionsData = queryClient.getQueryData(getListSessionsQueryKey()) as Session[] ?? [];
const today = new Date();
today.setHours(0, 0, 0, 0);
const todaysSessions = sessionsData.filter(s => new Date(s.createdAt) >= today);

const focusSessions = todaysSessions.filter(s => s.type === "simple" || s.type === "pomodoro_focus");
const breakSessions = todaysSessions.filter(s => s.type === "manual_break" || s.type === "pomodoro_break");

const totalFocusTime = focusSessions.reduce((a, s) => a + s.durationSeconds, 0);
const totalBreakTime = breakSessions.reduce((a, s) => a + s.durationSeconds, 0);

const formatMins = (secs: number) => {
  const h = Math.floor(secs / 3600);
  const m = Math.floor((secs % 3600) / 60);
  return h > 0 ? `${h}h ${m}m` : `${m}m`;
};

const sessionDetails = focusSessions.map(s => {
  const start = new Date(new Date(s.createdAt).getTime() - s.durationSeconds * 1000);
  return `- ${s.projectName ?? s.taskName ?? "Independent work"}: ${formatMins(s.durationSeconds)} (started ${start.toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit" })})`;
}).join('\n');

const breakDetails = breakSessions.map(s => {
  return `- ${s.notes?.trim() || "Pomodoro break"}: ${formatMins(s.durationSeconds)}`;
}).join('\n');

const firstSession = focusSessions.length > 0
  ? new Date(new Date(focusSessions[0].createdAt).getTime() - focusSessions[0].durationSeconds * 1000)
  : null;

const prompt = `You are a Chief of Staff AI coach for a high-performing executive. Based on the session data below, write a daily debrief summary.

Voice: Professional, insightful, energized. Like a senior CoS briefing a C-suite executive.
Tone: Encouraging but grounded in reality. Call out what worked, what didn't, and give one sharp forward-looking directive.
Style: Short punchy sentences. Growth-mindset vocabulary. Collaborative "we" occasionally.

Guidelines:
- Lead with the most significant insight, not chronology
- Acknowledge genuine effort without empty praise  
- Call out honestly: late starts (first session after 10am is late), long breaks (over 30 mins), task switching (4+ different projects), skewed focus/break ratio (less than 2:1 is concerning)
- Recognize momentum when sessions stack on the same task
- End with ONE specific actionable directive for tomorrow — make it a challenge not a compliment
- Never use "great job", "keep it up", or corporate clichés
- Write in 3-4 paragraphs maximum

Data for ${new Date().toLocaleDateString("en-US", { weekday: "long", year: "numeric", month: "long", day: "numeric" })}:

Day started: ${firstSession ? firstSession.toLocaleTimeString("en-US", { hour: "numeric", minute: "2-digit" }) : "No sessions recorded"}
Total focus time: ${formatMins(totalFocusTime)}
Total break time: ${formatMins(totalBreakTime)}
Focus to break ratio: ${totalBreakTime > 0 ? (totalFocusTime / totalBreakTime).toFixed(1) + ':1' : 'No breaks taken'}
Number of focus sessions: ${focusSessions.length}
Number of breaks: ${breakSessions.length}

Focus sessions:
${sessionDetails || "None"}

Breaks taken:
${breakDetails || "None"}

Session notes from the user:
${notes || "No notes recorded"}

Write the debrief now.`;

Do not change anything else.